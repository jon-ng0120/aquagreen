import React from 'react';
import HomeMessage from './HomeMessage';
import Hero from './Hero';

const Home = () => {
  return (
    <React.Fragment>
      <Hero />
      <HomeMessage />
    </React.Fragment>
  );
};

export default Home;
